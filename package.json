{
    "name": "arcticflower-fullstack",
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npm run build --configuration production",
        "start": "cd backend && npm install && node server.js"
    },
    "engines": {
        "node": "22.x"
    }
}